<UserControl x:Class="FiveMVehicleMetaEditorWPF.Views.Tabs.ValidatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource SecondaryBgBrush}" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding LoadCommand}" Content="📂 Load File" Width="100" Style="{StaticResource CommandButtonStyle}"/>
                <Button Command="{Binding ValidateCommand}" Content="✓ Validate" Width="100"/>
                <Button Command="{Binding ExportCommand}" Content="📤 Export Report" Width="130" Style="{StaticResource CommandButtonStyle}"/>
                <TextBox Text="{Binding SelectedFilePath}" Background="{DynamicResource DarkBgBrush}"
                         Foreground="{DynamicResource LightTextBrush}" Padding="8" Margin="10,0,0,0" Width="300"/>
            </StackPanel>
        </Border>

        <!-- Validation Results -->
        <Border Grid.Row="1" Background="{DynamicResource SecondaryBgBrush}" Margin="10" Padding="15">
            <StackPanel>
                <TextBlock Text="Validation Results" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,10"/>
                <ListBox ItemsSource="{Binding ValidationResults}" Background="{DynamicResource DarkBgBrush}"
                         Foreground="{DynamicResource LightTextBrush}" Height="400" Padding="10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,3" Foreground="{DynamicResource LightTextBrush}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource TertiaryBgBrush}" Padding="10">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource GrayTextBrush}"/>
        </Border>
    </Grid>
</UserControl>


