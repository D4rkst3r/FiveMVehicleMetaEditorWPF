<UserControl x:Class="FiveMVehicleMetaEditorWPF.Views.Tabs.HandlingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="{DynamicResource SecondaryBgBrush}" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding LoadCommand}" Content="📂 Load" Width="80" Style="{StaticResource CommandButtonStyle}"/>
                <Button Command="{Binding SaveCommand}" Content="💾 Save" Width="80" Style="{StaticResource CommandButtonStyle}"/>
                <Button Command="{Binding ExportCommand}" Content="📤 Export" Width="80" Style="{StaticResource CommandButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Left: Handling List -->
        <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource SecondaryBgBrush}" Margin="5">
            <StackPanel>
                <!-- Search -->
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource DarkBgBrush}" Foreground="{DynamicResource LightTextBrush}"
                         Padding="8" Margin="5" Height="30"/>
                <!-- Handling List -->
                <ListBox ItemsSource="{Binding HandlingEntries}"
                         SelectedItem="{Binding SelectedHandling}"
                         Background="{DynamicResource DarkBgBrush}"
                         Foreground="{DynamicResource LightTextBrush}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding HandlingName}" FontSize="11"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Right: Handling Properties Editor -->
        <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource SecondaryBgBrush}" Margin="5" Padding="15">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="Handling Parameters" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>

                    <StackPanel Visibility="{Binding SelectedHandling, Converter={StaticResource BoolToVisConverter}}">
                        <!-- Mass & Dimensions Section -->
                        <TextBlock Text="Mass &amp; Dimensions" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,15,0,10"/>

                        <Grid ColumnDefinitions="80,*,40" RowDefinitions="Auto,Auto,Auto,Auto" VerticalAlignment="Top">
                            <TextBlock Text="Mass:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Value="{Binding Mass}" Minimum="500" Maximum="5000" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding Mass, StringFormat=F0}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="1" Text="Dim X:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="1" Grid.Column="1" Value="{Binding DimensionsX}" Minimum="0.5" Maximum="5" Margin="10,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding DimensionsX, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="2" Text="Dim Y:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="2" Grid.Column="1" Value="{Binding DimensionsY}" Minimum="1" Maximum="8" Margin="10,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding DimensionsY, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="3" Text="Dim Z:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding DimensionsZ}" Minimum="0.5" Maximum="3" Margin="10,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding DimensionsZ, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>
                        </Grid>

                        <!-- Transmission Section -->
                        <TextBlock Text="Transmission" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,15,0,10"/>

                        <Grid ColumnDefinitions="80,*,40" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
                            <TextBlock Text="Accel X:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Value="{Binding AccelerationX}" Minimum="0.1" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding AccelerationX, StringFormat=F3}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="1" Text="Accel Y:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="1" Grid.Column="1" Value="{Binding AccelerationY}" Minimum="-1.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding AccelerationY, StringFormat=F3}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="2" Text="Accel Z:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="2" Grid.Column="1" Value="{Binding AccelerationZ}" Minimum="-1.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding AccelerationZ, StringFormat=F3}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="3" Text="Gears:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding NumberOfGears}" Minimum="3" Maximum="12" Margin="10,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding NumberOfGears, StringFormat=F0}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="4" Text="Top Speed:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="4" Grid.Column="1" Value="{Binding TopSpeed}" Minimum="50" Maximum="400" Margin="10,0"/>
                            <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding TopSpeed, StringFormat=F0}" Foreground="{DynamicResource GrayTextBrush}"/>
                        </Grid>

                        <!-- Steering & Braking Section -->
                        <TextBlock Text="Steering &amp; Braking" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,15,0,10"/>

                        <Grid ColumnDefinitions="80,*,40" RowDefinitions="Auto,Auto,Auto,Auto">
                            <TextBlock Text="Steer Lock:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Value="{Binding SteeringLock}" Minimum="10" Maximum="90" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding SteeringLock, StringFormat=F1}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="1" Text="Steer Bias:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="1" Grid.Column="1" Value="{Binding SteeringBias}" Minimum="-1.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding SteeringBias, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="2" Text="Brake Force:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="2" Grid.Column="1" Value="{Binding BrakeForce}" Minimum="0.1" Maximum="5.0" Margin="10,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding BrakeForce, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="3" Text="Brake Bias:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding BrakeBias}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding BrakeBias, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>
                        </Grid>

                        <!-- Suspension Section -->
                        <TextBlock Text="Suspension" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,15,0,10"/>

                        <Grid ColumnDefinitions="80,*,40" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
                            <TextBlock Text="Height:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Value="{Binding SuspensionHeight}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding SuspensionHeight, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="1" Text="Lower Lim:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="1" Grid.Column="1" Value="{Binding SuspensionLowerLimit}" Minimum="-1.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding SuspensionLowerLimit, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="2" Text="Upper Lim:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="2" Grid.Column="1" Value="{Binding SuspensionUpperLimit}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding SuspensionUpperLimit, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="3" Text="Stiffness:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding SuspensionStiffness}" Minimum="0.1" Maximum="5.0" Margin="10,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding SuspensionStiffness, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="4" Text="Damping:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="4" Grid.Column="1" Value="{Binding SuspensionDamping}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding SuspensionDamping, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>
                        </Grid>

                        <!-- Traction & Advanced Section -->
                        <TextBlock Text="Traction &amp; Advanced" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,15,0,10"/>

                        <Grid ColumnDefinitions="80,*,40" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
                            <TextBlock Text="Traction Max:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Value="{Binding TractionCurveMax}" Minimum="0.5" Maximum="3.0" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding TractionCurveMax, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="1" Text="Traction Min:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="1" Grid.Column="1" Value="{Binding TractionCurveMin}" Minimum="0.1" Maximum="2.0" Margin="10,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding TractionCurveMin, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="2" Text="Downforce:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="2" Grid.Column="1" Value="{Binding Downforce}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Downforce, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="3" Text="Roll Front:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding RollCentreHeightFront}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding RollCentreHeightFront, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>

                            <TextBlock Grid.Row="4" Text="Roll Rear:" Foreground="{DynamicResource LightTextBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Row="4" Grid.Column="1" Value="{Binding RollCentreHeightRear}" Minimum="0.0" Maximum="1.0" Margin="10,0"/>
                            <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding RollCentreHeightRear, StringFormat=F2}" Foreground="{DynamicResource GrayTextBrush}"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Visibility="{Binding SelectedHandling, Converter={StaticResource BoolToVisInverseConverter}}">
                        <TextBlock Text="Select a handling entry to edit" FontSize="11" Foreground="{DynamicResource GrayTextBrush}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="{StaticResource TertiaryBgBrush}" Padding="10">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource GrayTextBrush}"/>
        </Border>
    </Grid>
</UserControl>

